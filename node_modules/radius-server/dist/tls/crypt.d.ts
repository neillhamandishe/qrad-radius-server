/// <reference types="node" />
import * as events from 'events';
import * as tls from 'tls';
import { ILogger } from '../interfaces/Logger.js';
export interface ITLSServer {
    events: events.EventEmitter;
    tls: tls.TLSSocket;
}
export declare function startTLSServer(tlsOptions: tls.SecureContextOptions, logger: ILogger): ITLSServer;
export declare function encodeTunnelPW(key: Buffer, authenticator: Buffer, secret: string): Buffer;
