export class StaticAuth {
    constructor(options, logger) {
        this.logger = logger.context('StaticAuth');
        this.validCredentials = options.validCredentials;
    }
    async authenticate(username, password) {
        return !!this.validCredentials.find((credential) => credential.username === username && credential.password === password);
    }
}
//# sourceMappingURL=StaticAuth.js.map