import { IAuthentication } from '../../interfaces/Authentication.js';
import { IPacket, IPacketHandler, IPacketHandlerResult } from '../../interfaces/PacketHandler.js';
import { ILogger } from '../../interfaces/Logger.js';
export declare class UserPasswordPacketHandler implements IPacketHandler {
    private authentication;
    private logger;
    constructor(authentication: IAuthentication, logger: ILogger);
    handlePacket(packet: IPacket): Promise<IPacketHandlerResult>;
}
