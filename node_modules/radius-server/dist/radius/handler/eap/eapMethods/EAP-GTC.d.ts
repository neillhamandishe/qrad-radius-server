/// <reference types="node" />
import { IPacketHandlerResult } from '../../../../interfaces/PacketHandler.js';
import { IEAPMethod } from '../../../../interfaces/EAPMethod.js';
import { IAuthentication } from '../../../../interfaces/Authentication.js';
import { ILogger } from '../../../../interfaces/Logger.js';
export declare class EAPGTC implements IEAPMethod {
    private authentication;
    private logger;
    getEAPType(): number;
    extractValue(msg: Buffer): Buffer;
    identify(identifier: number, _stateID: string): IPacketHandlerResult;
    constructor(authentication: IAuthentication, logger: ILogger);
    handleMessage(_identifier: number, _stateID: string, msg: Buffer, _: any, identity?: string): Promise<IPacketHandlerResult>;
}
