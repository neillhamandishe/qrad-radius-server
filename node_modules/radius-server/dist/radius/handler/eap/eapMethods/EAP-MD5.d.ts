/// <reference types="node" />
import { RadiusPacket } from 'radius';
import { IPacketHandlerResult } from '../../../../interfaces/PacketHandler.js';
import { IEAPMethod } from '../../../../interfaces/EAPMethod.js';
import { IAuthentication } from '../../../../interfaces/Authentication.js';
import { ILogger } from '../../../../interfaces/Logger.js';
export declare class EAPMD5 implements IEAPMethod {
    private authentication;
    private logger;
    getEAPType(): number;
    identify(_identifier: number, _stateID: string): IPacketHandlerResult;
    constructor(authentication: IAuthentication, logger: ILogger);
    handleMessage(_identifier: number, _stateID: string, _msg: Buffer, _orgRadiusPacket: RadiusPacket): Promise<IPacketHandlerResult>;
}
