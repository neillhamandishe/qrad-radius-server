import { IPacket, IPacketHandler, IPacketHandlerResult } from '../../interfaces/PacketHandler.js';
import { IEAPMethod } from '../../interfaces/EAPMethod.js';
import { ILogger } from '../../interfaces/Logger.js';
export declare class EAPPacketHandler implements IPacketHandler {
    private eapMethods;
    private identities;
    private eapConnectionStates;
    private logger;
    constructor(eapMethods: IEAPMethod[], logger: ILogger);
    handlePacket(packet: IPacket, handlingType?: number): Promise<IPacketHandlerResult>;
}
