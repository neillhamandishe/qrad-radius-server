/// <reference types="node" />
import { EventEmitter } from 'events';
import { UDPServer } from '../server/UDPServer.js';
import { RadiusServerOptions } from '../interfaces/RadiusServerOptions.js';
export declare class RadiusServer extends UDPServer {
    private options;
    private packetHandler;
    constructor(options: RadiusServerOptions);
    start(): Promise<EventEmitter>;
    private installListeners;
    handleMessage(msg: Buffer): Promise<{
        data: Buffer;
        expectAcknowledgment?: boolean;
    } | undefined>;
}
