/// <reference types="node" />
import * as tls from 'tls';
import { IPacket, IPacketHandler, IPacketHandlerResult } from '../interfaces/PacketHandler.js';
import { IAuthentication } from '../interfaces/Authentication.js';
import { ILogger } from '../interfaces/Logger.js';
export declare class PacketHandler implements IPacketHandler {
    private secret;
    private vlan?;
    packetHandlers: IPacketHandler[];
    private logger;
    constructor(authentication: IAuthentication, tlsOptions: tls.SecureContextOptions, logger: ILogger, secret: string, vlan?: number | undefined);
    handlePacket(packet: IPacket, handlingType?: number): Promise<IPacketHandlerResult>;
}
