import { ISyncKeyStrategy } from '../../types/key.strategy.types.js';
export declare class JSONStringifyKeyStrategy implements ISyncKeyStrategy {
    getKey(className: string, methodName: string, args: any[]): string;
}
